version: '2.1'
volumes:
    dnsfiles: {}
services:
  dnsmasq:
    image: irscythe/dnsmasq-arm
    cap_add:
      - NET_ADMIN 
    expose:
      - 5432
    ports: 
      - 53:53/udp
      - 5380:8080
    volumes:
      - 'dnsfiles:/etc/'
    environment:
      HTTP_USER: moquito64
      HTTP_PASS: Xavier64!

