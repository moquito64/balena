version: '2.1'
volumes:
  pgres-data: {}
services:
  db:
    image: rotschopf/rpi-postgres
    expose:
      - '5432'
    ports:
      - '5432:5432'
    volumes:
      - 'pgres-data:/var/lib/postgresql/'
    environment:
      POSTGRES_PASSWORD: DragonQuest64!
